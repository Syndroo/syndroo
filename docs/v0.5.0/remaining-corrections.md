# 待授权的补正范围

这些范围的初始三次执行尝试已经用完，仍有未验收缺口。项目 AGENTS.md 要求用尽尝试后停止继续执行，并由用户明确修订尝试预算。下表是待审阅的补正方案，不代表已经授权或已经完成；其他独立开发可以继续。T9a 的隔离与有限退出机制已获局部验收，但其测试生命周期问题尚未解决。

| 范围 | 已确认缺口 | 拟追加预算与交付 |
| --- | --- | --- |
| T2 传输与平台适配器 | Retry-After 接受不存在的日期；合法的较晚等待被截短为24小时；原生测试的 `.ts` 导入导致类型检查失败；证据文件过时。 | 追加1次：修正日期及等待规则、导入与证据；运行传输、五平台及原生 workerd 测试和类型检查。保留有效的较晚 Retry-After，不减少原验收要求。 |
| T3 D1 语义仓库 | 恢复 SQL 部分围栏未齐；OAuth/outcome 提交成功但回执丢失、既有 parent 零行 guard 和终结竞争证据不足；读行数、20-job 真实开销、查询计划、旧库迁移演练未完成；新仓库尚无公开帖子读取投影。 | 追加3个顺序步骤，每步1次：①补围栏与真实原子性/回执丢失回归；②采集实际语句、读写行数和查询计划，确定运行批量；③补 fresh/legacy 迁移、公开 list/get 投影及证据。运行时最终收敛为同一个具体仓库。 |
| T4 加密与 R2 | 尚无真正 host Node 与 workerd 双向 AES 互通；新存储边界缺专门回归；错误、取消、缓冲区复用、元数据/校验和、key/keyId、HMAC 输入快照与测试类型证据不全。 | 追加3个顺序步骤，每步1次：①真实双运行时互通；②R2、key/HMAC 边界回归；③修复测试类型并复跑完整专门套件、更新证据。不能用 workerd 内的 node:crypto 兼容层代替 host Node。 |
| T7c1 CLI 鉴权命令 | 三轮后通过135项测试，但仍无慢速超限输入且管道保持打开时的自然退出证明；doctor 在 readiness 请求过程中取消、Tumblr 显式完成目标和部分失败读取计数的证据未齐。随包 Skill 的独立范围不包含这些缺口。 | 拟追加1次，仅补上述有限回归及其发现的必要修正，复跑 CLI 检查与完整测试。此项在上一份三组预算问题之后新增，尚未获授权。 |
| T9a Worker 测试生命周期 | 主项目虽返回 exit0，仍出现 `EnvironmentTeardownError`。移除自动 Queue consumer 可消除小范围复现，但导致4项真实消费编排测试超时；第3次尝试已恢复所需配置，未屏蔽失败。 | 拟追加1次：将确实依赖自动消费的编排测试放入专用项目，其余项目保留 producer 和手动消费者测试；校验每项测试恰归属一个项目，并完整复跑、确认退出日志和进程清理。若拆分仍不能解决，则保留原断言并报告原因，不以绿色退出码代替生命周期证明。此方案尚未实施或授权。 |

实际问题和先前测试记录见 [独立复核](evidence/root-review.md)。通过已有测试不能替代缺失的边界证据；上述范围继续保持未验收。补正只涉及本地代码、假凭据、一次性数据库和隔离出站测试，不包含部署、线上迁移、真实平台调用或包发布。授权修订尝试次数也不构成这些外部操作的授权。

后续接线仍须通过 HTTP/Queue/Cron、迁移、安装产物和全部本地验收门槛。没有将这些未决范围改名为“集成”来绕过原预算。
